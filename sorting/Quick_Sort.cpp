#include <bits/stdc++.h>
using namespace std;

class Solution{
    public:
    int sort(vector<int>& arr){
        
    }
};

int main(){
    int n;
    vector<int>arr;
    cin>>n;
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }
    Solution s;
    s.sort(arr);
    return 0;
}